<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:id="@+id/menu_main_category" android:menuCategory="system">
        <item
            android:id="@+id/menu_exit"
            app:showAsAction="withText"
            android:title="@string/menu_option_exit"
            android:icon="@android:drawable/ic_menu_close_clear_cancel"
            />
    </group>

    <group android:id="@+id/menu_group_container" android:menuCategory= "container"
        android:enabled="false">


        <!-- This item is not used at the moment -->
        <item android:id="@+id/search"
            android:visible="false"
            android:title="@string/app_bar_search"
            android:icon="@android:drawable/ic_menu_search"
            app:showAsAction="collapseActionView|ifRoom"
            android:actionViewClass="android.widget.SearchView" />

    </group>

</menu>